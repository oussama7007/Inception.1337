# Inception.1337

Containers vs VMs vs processes — why Docker exists at all
Images, layers, Dockerfiles — why no latest tag, why a specific base image
PID 1 and exec — why no tail -f / sleep infinity
Env vars vs Docker secrets — the confidentiality model
Docker networking — bridge networks, service discovery, why --link/host are banned
Volumes vs bind mounts — persistence model
TLS basics — what "TLS 1.2/1.3 only" actually means
The 3-tier architecture — nginx → php-fpm → mariadb, and why php-fpm has no bundled server
WordPress specifics — wp-config, wp-cli, the two-user requirement
Docker Compose orchestration — how the pieces get wired together
Full request lifecycle — tracing one HTTP request through the whole stack
Defense mechanics — how evaluators probe, and the live-modification test

